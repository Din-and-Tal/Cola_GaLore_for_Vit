optuna:
  study_name: "vit_hpo"
  direction: "minimize"  # Changed from maximize to minimize val_loss
  n_trials: 20
  n_jobs: 1
  sampler: "tpe"  # Tree-structured Parzen Estimator
  
  # Pruner configuration
  pruner: "median"      # MedianPruner is robust
  n_startup_trials: 5   # Let the first 5 trials run fully to establish a baseline
  n_warmup_steps: 10    # Don't prune the first 10 epochs of any trial

  search_space:
    cola_rank_ratio: [0, 1] 
    # 1. Core optimization (around your working values)
    # scheduler_max_lr: [1e-4, 6e-4]    # you liked 3e-4 → center the range here
    # weight_decay: [0.02, 0.12]     # around your 0.06 (DeiT-like region)

    # # 2. Regularization
    # label_smoothing: [0.0, 0.10]   # don’t go crazy; mixup also softens labels
    # hidden_dropout_prob: [0.0, 0.3]

    # # 3. Data mixing
    # mixup_alpha: [0.0, 0.30]       # 0.0 = off; 0.1–0.2 typical
    # cutmix_alpha: [0.0, 0.20]      # 0.0 = off; keep lower than mixup
    # mix_prob: [0.0, 0.70]